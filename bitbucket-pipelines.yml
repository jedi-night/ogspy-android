# This is a sample build configuration for Gradle.
# Check our guides at https://confluence.atlassian.com/x/VYk8Lw for more examples.
# Only use spaces to indent your .yml configuration.
# -----
# You can specify a custom docker image from Docker Hub as your build environment.
image: uber/android-build-environment:latest

pipelines:
  default:
    - step:
        script: # Modify the commands below to build your repository.
          # Grab the Android Support Repo which isn't included in the container
          # android update sdk --filter "extra-android-m2repository" --no-ui -a 
          - echo y | android update sdk --no-ui -a --filter build-tools-24.0.0,android-24,extra-android-m2repository
          - ./gradlew --stacktrace --debug assembleDebug